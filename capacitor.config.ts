import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'dark-web-manager-electron',
  webDir: 'dist'
};

export default config;
